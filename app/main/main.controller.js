(function(angular) {
  angular.module('nvision.main', ['ui.router'])
  .controller('MainCtrl', ['$scope', '$state', '$rootScope', MainCtrl]);

  function MainCtrl($scope, $state, $rootScope){
    
  }

})(window.angular);